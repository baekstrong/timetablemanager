# 입학반 수동 등록 + 관련 버그 수정 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 코치가 `StudentRegistrationModal`에서 신규 수강생을 입학반과 함께 등록·SMS 발송할 수 있게 하고, 입학반 관리 UX의 잡음(이미 입학반 다녀온 학생 노출, 지난 입학반 카드)을 정리한다.

**Architecture:** 모든 변경은 React 컴포넌트 단의 수정. 새 backend/firestore 스키마 변경 없음. 기존 `sendApprovalNotifications`, `getEntranceClasses`, `updateEntranceClass`, `createNewStudentRegistration` 함수를 재사용. 변경 파일은 3개(`StudentRegistrationModal.jsx`, `CoachNewStudents.jsx`, `CoachNewStudents.css`).

**Tech Stack:** React 19, Vite, Firebase Firestore, Google Sheets API. 자동화된 테스트 인프라 없음 → 각 Task는 코드 변경 → `npm run dev`로 수동 검증 → 커밋 사이클로 진행.

**Spec:** `docs/superpowers/specs/2026-04-22-입학반-수동-등록-design.md`

---

## File Structure

| 파일 | 역할 | 변경 유형 |
| --- | --- | --- |
| `src/components/StudentRegistrationModal.jsx` | 수강생 등록 모달 — 입학반 셀렉터, SMS 호출, prop 추가 | 수정 |
| `src/components/CoachNewStudents.jsx` | 신규 수강생 관리 — "+ 직접 등록" 버튼, 필터 강화, 지난 입학반 접힘 | 수정 |
| `src/components/CoachNewStudents.css` | 접힘 토글 스타일 | 수정 |

신규 파일 없음.

---

## Task 1: 수강생 추가 모달에서 "이미 지난 입학반에 다녀온 신규 학생" 제외

**목표:** Image #5의 버그 — 이미 입학반을 마친 정윤희·주상조가 다음 입학반의 "수강생 추가" 모달에 계속 뜨는 문제 해결.

**Files:**
- Modify: `src/components/CoachNewStudents.jsx:779-816` (`handleOpenAddStudent` 함수)

- [ ] **Step 1: `handleOpenAddStudent`에서 모든 등록 이력을 조회하고 완료된 입학반 다녀온 이름 Set 만들기**

`src/components/CoachNewStudents.jsx`의 `handleOpenAddStudent` 함수(약 779–816 라인)를 다음으로 교체:

```javascript
const handleOpenAddStudent = async (ec) => {
    setShowAddStudentModal(ec);
    setSelectedNewStudents(new Set());
    setAddStudentLoading(true);
    try {
        const targetSheet = getCurrentSheetName();
        const [rows, allRegs] = await Promise.all([
            readSheetData(`${targetSheet}!A:R`),
            getNewStudentRegistrations(null).catch(() => [])
        ]);

        // 이미 지난 입학반에 배정된 적 있는 신규 학생 이름 수집
        const today = new Date();
        today.setHours(0, 0, 0, 0);
        const completedEntranceNames = new Set();
        for (const r of allRegs) {
            if (!r.name || !r.entranceClassId || !r.entranceDate) continue;
            const d = new Date(r.entranceDate + 'T23:59:59');
            if (d < today) completedEntranceNames.add(r.name);
        }

        // 이미 이 입학반에 등록된 수강생 이름 목록
        const existingNames = new Set(
            entranceRegs.filter(r => r.entranceClassId === ec.id).map(r => r.name)
        );

        // F열(index 5)이 '신규'인 수강생 필터링
        const newStudents = [];
        for (let i = 2; i < rows.length; i++) {
            const row = rows[i];
            if (!row || !row[1]) continue;
            if (row[5] !== '신규') continue;
            const name = row[1];
            if (existingNames.has(name)) continue;
            if (completedEntranceNames.has(name)) continue;
            newStudents.push({
                rowIndex: i,
                name,
                weeklyFrequency: row[2] || '',
                schedule: row[3] || '',
                startDate: row[6] || '',
                phone: row[15] || ''
            });
        }
        setSheetNewStudents(newStudents);
    } catch (err) {
        console.error('신규 수강생 목록 로드 실패:', err);
        alert('신규 수강생 목록을 불러오지 못했습니다.');
        setShowAddStudentModal(null);
    }
    setAddStudentLoading(false);
};
```

- [ ] **Step 2: 수동 검증**

```bash
npm run dev
```

브라우저에서 코치 계정 로그인 → 신규 수강생 관리 → 입학반 관리 탭 → 다가오는 입학반 카드의 "+" 클릭. 정윤희·주상조처럼 이미 지난 입학반에 다녀온 학생이 리스트에서 사라졌는지 확인.

- [ ] **Step 3: 커밋**

```bash
git add src/components/CoachNewStudents.jsx
git commit -m "fix: 수강생 추가 모달에서 이미 지난 입학반 다녀온 신규 학생 제외"
```

---

## Task 2: 입학반 관리 — 지난 입학반 접힘 섹션

**목표:** Image #6의 4월 18일처럼 이미 지난 입학반 카드를 페이지 하단 접힘 섹션으로 모은다.

**Files:**
- Modify: `src/components/CoachNewStudents.jsx` (state 추가 + 입학반 관리 탭 렌더링 수정, 약 1247–1334 라인)
- Modify: `src/components/CoachNewStudents.css` (접힘 토글 스타일 추가)

- [ ] **Step 1: 접힘 토글 state 추가**

`src/components/CoachNewStudents.jsx`의 입학반 관리 state 블록(약 56–66 라인) 끝에 추가:

```javascript
// 지난 입학반 접힘 토글
const [showPastEntrance, setShowPastEntrance] = useState(false);
```

- [ ] **Step 2: 입학반 관리 탭 렌더링을 다가오는/지난 분리 구조로 변경**

`src/components/CoachNewStudents.jsx`의 `{/* === 입학반 관리 탭 === */}` 블록 안에서, `entranceClasses.map(ec => ...)` 부분(약 1268–1334 라인)을 다음 구조로 교체. 카드 렌더링 함수를 추출해 두 곳에서 재사용한다.

```jsx
{loading ? (
    <div className="cns-loading">불러오는 중...</div>
) : entranceClasses.length === 0 ? (
    <div className="cns-empty">등록된 입학반이 없습니다.</div>
) : (() => {
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    const upcoming = [];
    const past = [];
    for (const ec of entranceClasses) {
        if (!ec.date) { upcoming.push(ec); continue; }
        const ecDate = new Date(ec.date + 'T23:59:59');
        if (ecDate < today) past.push(ec);
        else upcoming.push(ec);
    }
    upcoming.sort((a, b) => (a.date || '').localeCompare(b.date || ''));
    past.sort((a, b) => (b.date || '').localeCompare(a.date || ''));

    const renderEcCard = (ec) => (
        <div key={ec.id} className={`cns-entrance-card ${!ec.isActive ? 'inactive' : ''}`}>
            <div className="cns-entrance-info">
                <div className="cns-entrance-date">{formatEntranceDate(ec.date)}</div>
                <div className="cns-entrance-time">{ec.time}{ec.endTime ? ` ~ ${ec.endTime}` : ''}</div>
                {ec.description && <div className="cns-entrance-desc">{ec.description}</div>}
                <div className="cns-entrance-capacity">
                    {ec.currentCount || 0}/{ec.maxCapacity}명
                    {!ec.isActive && <span className="cns-inactive-badge">비활성</span>}
                </div>
                {(() => {
                    const ecRegs = entranceRegs.filter(r => r.entranceClassId === ec.id);
                    if (ecRegs.length === 0) return null;
                    return (
                        <div className="cns-entrance-students">
                            {ecRegs.map(r => (
                                <span key={r.id} className={`cns-entrance-student-tag ${r.status}`}>
                                    {r.name}
                                    {r.status === 'pending' && <small>(대기)</small>}
                                    <button
                                        className="cns-entrance-student-remove"
                                        onClick={() => handleDeleteFromEntrance(r, ec)}
                                        title="삭제"
                                    >×</button>
                                </span>
                            ))}
                        </div>
                    );
                })()}
            </div>
            <div className="cns-entrance-actions">
                <button
                    className="cns-icon-btn add"
                    onClick={() => handleOpenAddStudent(ec)}
                    title="수강생 추가"
                >
                    +
                </button>
                <button
                    className="cns-icon-btn edit"
                    onClick={() => {
                        setEditingEntrance(ec);
                        setEntranceForm({
                            date: ec.date,
                            time: ec.time,
                            endTime: ec.endTime || '',
                            description: ec.description || '',
                            maxCapacity: ec.maxCapacity || 10,
                            currentCount: ec.currentCount || 0
                        });
                        setShowEntranceForm(true);
                    }}
                >
                    ✏️
                </button>
                <button
                    className="cns-icon-btn delete"
                    onClick={() => handleEntranceDelete(ec)}
                >
                    🗑️
                </button>
            </div>
        </div>
    );

    return (
        <>
            <div className="cns-entrance-list">
                {upcoming.length === 0 ? (
                    <div className="cns-empty">다가오는 입학반이 없습니다.</div>
                ) : upcoming.map(renderEcCard)}
            </div>
            {past.length > 0 && (
                <div className="cns-past-entrance-section">
                    <button
                        className="cns-past-entrance-toggle"
                        onClick={() => setShowPastEntrance(v => !v)}
                    >
                        {showPastEntrance ? '▲' : '▼'} 지난 입학반 ({past.length})
                    </button>
                    {showPastEntrance && (
                        <div className="cns-entrance-list past">
                            {past.map(renderEcCard)}
                        </div>
                    )}
                </div>
            )}
        </>
    );
})()}
```

- [ ] **Step 3: 접힘 토글 스타일 추가**

`src/components/CoachNewStudents.css` 끝에 추가:

```css
.cns-past-entrance-section {
    margin-top: 1rem;
}

.cns-past-entrance-toggle {
    width: 100%;
    padding: 0.7rem 1rem;
    border: 1px solid #e5e7eb;
    border-radius: 8px;
    background: #f9fafb;
    color: #4b5563;
    font-size: 0.9rem;
    font-weight: 600;
    cursor: pointer;
    text-align: left;
    transition: background 0.15s ease;
}

.cns-past-entrance-toggle:hover {
    background: #f3f4f6;
    color: #1f2937;
}

.cns-entrance-list.past {
    margin-top: 0.5rem;
    opacity: 0.85;
}
```

- [ ] **Step 4: 수동 검증**

`npm run dev`가 이미 실행 중이면 자동 리로드. 입학반 관리 탭에서:
1. 다가오는 입학반(예: 5월 16일)이 상단에 표시됨.
2. 지난 입학반(예: 4월 18일) 카드가 상단 목록에서 사라지고, 하단에 `▼ 지난 입학반 (N)` 토글 버튼 등장.
3. 토글 클릭 시 지난 입학반 카드가 펼쳐짐. 다시 클릭하면 접힘.
4. 다가오는 입학반이 0개면 "다가오는 입학반이 없습니다." 메시지 + 지난 입학반 토글만 표시.

- [ ] **Step 5: 커밋**

```bash
git add src/components/CoachNewStudents.jsx src/components/CoachNewStudents.css
git commit -m "feat: 입학반 관리 페이지 지난 입학반 접힘 섹션"
```

---

## Task 3: StudentRegistrationModal — 입학반 셀렉터 필드 추가 (UI만)

**목표:** 신규 모드일 때 입학반을 선택할 수 있는 `<select>`를 추가. 등록 로직 연결은 Task 4에서 처리.

**Files:**
- Modify: `src/components/StudentRegistrationModal.jsx`

- [ ] **Step 1: 입학반 데이터 로드 + state + prop 추가**

`src/components/StudentRegistrationModal.jsx` 상단 import에 `getEntranceClasses` 추가:

```javascript
// line 14 부근 수정
import { getHolidays, createRenewalContract, createNewStudentRegistration, getEntranceClasses, updateEntranceClass } from '../services/firebaseService';
```

함수 시그니처(라인 66)에 `initialEntranceId` prop 추가:

```javascript
const StudentRegistrationModal = ({ onClose, onSuccess, initialRenewalName, initialEntranceId }) => {
```

state 블록(라인 71 근처) 아래에 입학반 state 추가:

```javascript
const [entranceClasses, setEntranceClasses] = useState([]);
const [selectedEntranceId, setSelectedEntranceId] = useState('');
```

- [ ] **Step 2: 입학반 로드 useEffect 추가**

라인 96 부근 `useEffect`(시트 로드) 아래에 새 effect 추가:

```javascript
// 입학반 목록 로드 (신규 모드 전용)
useEffect(() => {
    if (registrationType !== 'new') return;
    getEntranceClasses(false).then(list => {
        const today = new Date();
        today.setHours(0, 0, 0, 0);
        const upcoming = (list || [])
            .filter(ec => {
                if (!ec.isActive || !ec.date) return false;
                const d = new Date(ec.date + 'T23:59:59');
                return d >= today;
            })
            .sort((a, b) => a.date.localeCompare(b.date));
        setEntranceClasses(upcoming);

        // 기본값: initialEntranceId 우선, 없으면 만석이 아닌 가장 가까운 ec
        if (initialEntranceId && upcoming.some(ec => ec.id === initialEntranceId)) {
            setSelectedEntranceId(initialEntranceId);
        } else {
            const firstAvailable = upcoming.find(
                ec => (ec.currentCount || 0) < (ec.maxCapacity || 0)
            );
            setSelectedEntranceId(firstAvailable ? firstAvailable.id : '');
        }
    }).catch(err => console.error('입학반 조회 실패:', err));
}, [registrationType, initialEntranceId]);
```

- [ ] **Step 3: 입학반 셀렉터 필드 UI 추가**

신규 모드의 추가 개인정보 블록(약 라인 732, `{registrationType === 'new' && (`) **위쪽**, 결제일 블록(약 라인 714) **아래쪽**에 셀렉터 추가:

기존:
```jsx
{/* 결제일 */}
<div className="reg-field-group">
    <label>결제일</label>
    ...
</div>

{/* 신규 모드에서만 추가 개인정보 */}
{registrationType === 'new' && (
```

다음으로 변경 (결제일 블록과 신규 추가 개인정보 블록 사이에 셀렉터 삽입):

```jsx
{/* 결제일 */}
<div className="reg-field-group">
    <label>결제일</label>
    ...
</div>

{/* 입학반 선택 (신규 모드 전용) */}
{registrationType === 'new' && (
    <div className="reg-field-group">
        <label>입학반</label>
        <select
            value={selectedEntranceId}
            onChange={(e) => setSelectedEntranceId(e.target.value)}
        >
            <option value="">선택 안 함</option>
            {entranceClasses.map(ec => {
                const isFull = (ec.currentCount || 0) >= (ec.maxCapacity || 0);
                const dateLabel = formatEntranceDate(ec.date);
                const timeLabel = `${ec.time || ''}${ec.endTime ? ` ~ ${ec.endTime}` : ''}`.trim();
                const capacityLabel = `${ec.currentCount || 0}/${ec.maxCapacity || 0}명`;
                return (
                    <option key={ec.id} value={ec.id} disabled={isFull && ec.id !== selectedEntranceId}>
                        {dateLabel} {timeLabel} ({capacityLabel}){isFull ? ' (만석)' : ''}
                    </option>
                );
            })}
        </select>
        <div className="field-hint">기본값은 가장 가까운 다가오는 입학반입니다. "선택 안 함" 시 입학반 미연결로 등록됩니다.</div>
    </div>
)}

{/* 신규 모드에서만 추가 개인정보 */}
{registrationType === 'new' && (
```

`formatEntranceDate`는 `dateUtils`에서 import 필요. 라인 14 근처 import 추가/수정:

```javascript
import { formatEntranceDate } from '../utils/dateUtils';
```

- [ ] **Step 4: 수동 검증**

`npm run dev` 새로고침. 코치 → 수강생 관리 → "+ 등록" → 신규 모드에서:
1. 결제일 아래 "입학반" 셀렉터가 표시됨.
2. 다가오는 활성 입학반들이 옵션으로 나옴 (`5월 16일(토) 10:00 ~ 13:00 (2/6명)` 형식).
3. 만석 입학반은 disabled.
4. 기본값이 가장 가까운 다가오는 입학반으로 자동 세팅됨.
5. 재등록 모드 전환 시 입학반 셀렉터가 사라짐.

- [ ] **Step 5: 커밋**

```bash
git add src/components/StudentRegistrationModal.jsx
git commit -m "feat: StudentRegistrationModal 신규 모드에 입학반 선택 UI 추가"
```

---

## Task 4: StudentRegistrationModal — 등록 시 입학반 연결 처리

**목표:** Task 3에서 추가한 셀렉터 값을 실제 등록 흐름에 반영. Firestore registration 문서에 입학반 필드 저장 + `currentCount` 증가.

**Files:**
- Modify: `src/components/StudentRegistrationModal.jsx` (`handleSubmit` 함수)

- [ ] **Step 1: `handleSubmit`에서 입학반 검증 + 데이터 포함**

`src/components/StudentRegistrationModal.jsx`의 `handleSubmit` 함수(라인 274 시작)에서 `createNewStudentRegistration` 호출 부분(약 365–380 라인)을 다음 흐름으로 확장.

먼저 시트 쓰기 직후, `createNewStudentRegistration` 호출 전에 입학반 만석 재검증 추가:

```javascript
// 입학반 연결 처리 (신규 모드 전용)
let linkedEntrance = null;
let linkedEntranceClassDate = '';
if (registrationType === 'new' && selectedEntranceId) {
    try {
        const ecs = await getEntranceClasses(false);
        const ec = ecs.find(c => c.id === selectedEntranceId);
        if (!ec) {
            alert('선택한 입학반이 더 이상 존재하지 않습니다. 입학반 선택을 변경해주세요.');
            setSubmitting(false);
            return;
        }
        if ((ec.currentCount || 0) >= (ec.maxCapacity || 0)) {
            alert('선택한 입학반이 만석입니다. 다른 입학반을 선택해주세요.');
            setSubmitting(false);
            return;
        }
        linkedEntrance = ec;
        linkedEntranceClassDate = `${formatEntranceDate(ec.date)} ${ec.time || ''}${ec.endTime ? ' ~ ' + ec.endTime : ''}`.trim();
    } catch (ecErr) {
        console.warn('입학반 검증 실패:', ecErr);
    }
}
```

이후 기존 `createNewStudentRegistration` 호출(약 366–380 라인)을 다음으로 교체:

```javascript
// 신규 수강생 관리 페이지에 승인 이력 남기기
let createdRegId = null;
try {
    const regPayload = {
        name: form.이름,
        phone: form.핸드폰 || '',
        gender: form.성별 || '',
        occupation: form.직업 || '',
        weeklyFrequency: parseInt(form.주횟수),
        scheduleString: form['요일 및 시간'],
        paymentMethod: form.결제방식 || '',
        registeredByCoach: true,
        approvedAt: new Date()
    };
    if (linkedEntrance) {
        regPayload.entranceClassId = linkedEntrance.id;
        regPayload.entranceDate = linkedEntrance.date;
        regPayload.entranceClassDate = linkedEntranceClassDate;
    }
    const createResult = await createNewStudentRegistration(regPayload, 'approved');
    createdRegId = createResult?.id || null;
} catch (regErr) {
    console.warn('신규 수강생 이력 저장 실패 (시트 등록은 완료):', regErr);
}

// 입학반 currentCount 증가
if (linkedEntrance) {
    try {
        await updateEntranceClass(linkedEntrance.id, {
            currentCount: (linkedEntrance.currentCount || 0) + 1
        });
    } catch (ecUpdateErr) {
        console.warn('입학반 인원 업데이트 실패:', ecUpdateErr);
    }
}
```

- [ ] **Step 2: 수동 검증**

`npm run dev` 새로고침. 코치 → 수강생 관리 → "+ 등록" → 신규로 테스트 학생 등록 (입학반 선택). Firebase 콘솔에서 `newStudentRegistrations` 새 문서를 열어 다음 확인:
- `entranceClassId`, `entranceDate`, `entranceClassDate` 저장됨
- `status: 'approved'`, `registeredByCoach: true`

`entranceClasses` 컬렉션의 해당 입학반 문서에서 `currentCount`가 1 증가했는지 확인.

"선택 안 함"으로 등록 시:
- registration 문서에 `entranceClassId` 등이 없음
- 어떤 입학반의 `currentCount`도 변하지 않음

- [ ] **Step 3: 커밋**

```bash
git add src/components/StudentRegistrationModal.jsx
git commit -m "feat: StudentRegistrationModal 등록 시 입학반 연결 + 인원 증가"
```

---

## Task 5: StudentRegistrationModal — SMS 발송 (옵션 A)

**목표:** 신규 모드 + 핸드폰 입력됨 → 학생에게 승인 SMS(SMS 2) 발송, 입학반 연결 시 3일 전 리마인더 SMS(SMS 3) 예약.

**Files:**
- Modify: `src/components/StudentRegistrationModal.jsx`

- [ ] **Step 1: smsService import 및 결제방식 매핑 상수 추가**

`src/components/StudentRegistrationModal.jsx` import 블록(라인 14 근처)에 추가:

```javascript
import { sendApprovalNotifications } from '../services/smsService';
```

파일 상단(라인 16 `import './...css';` 다음, 컴포넌트 바깥)에 매핑 상수 추가:

```javascript
// 결제방식 한글 → SMS 코드 매핑 (smsService 호환)
const PAYMENT_CODE_MAP = {
    '네이버': 'naver',
    '카드': 'card',
    '제로페이': 'zeropay',
    '계좌': 'cash'
};
```

- [ ] **Step 2: handleSubmit 끝부분에 SMS 발송 + groupId 저장 로직 추가**

`src/components/StudentRegistrationModal.jsx`의 `handleSubmit` 끝부분, `alert('수강생이 등록되었습니다.');` **직전**에 추가:

```javascript
// SMS 발송 (신규 모드 + 핸드폰 입력 시)
let smsWarning = '';
if (registrationType === 'new' && form.핸드폰) {
    try {
        const smsResults = await sendApprovalNotifications(form.핸드폰, form.이름, {
            paymentMethod: PAYMENT_CODE_MAP[form.결제방식] || 'cash',
            weeklyFrequency: parseInt(form.주횟수) || 0,
            scheduleString: form['요일 및 시간'] || '',
            entranceDate: linkedEntrance ? linkedEntrance.date : '',
            entranceClassDate: linkedEntranceClassDate
        });

        const failed = [];
        if (!smsResults.approvalSMS) failed.push('승인 문자');
        if (linkedEntrance && smsResults.reminderSMS) {
            const groupId = typeof smsResults.reminderSMS === 'object'
                ? smsResults.reminderSMS.groupId
                : null;
            if (groupId && createdRegId) {
                try {
                    await updateNewStudentRegistration(createdRegId, { reminderGroupId: groupId });
                } catch (idErr) {
                    console.warn('reminderGroupId 저장 실패:', idErr);
                }
            }
        } else if (linkedEntrance && !smsResults.reminderSMS) {
            failed.push('입학반 리마인더');
        }
        if (failed.length > 0) {
            smsWarning = `\n\n⚠ ${failed.join(', ')} 발송에 실패했습니다.`;
        }
    } catch (smsError) {
        console.error('SMS 발송 오류:', smsError);
        smsWarning = '\n\n⚠ 안내 문자 발송에 실패했습니다.';
    }
}

alert('수강생이 등록되었습니다.' + smsWarning);
```

`updateNewStudentRegistration` import도 추가 (라인 14):

```javascript
import { getHolidays, createRenewalContract, createNewStudentRegistration, getEntranceClasses, updateEntranceClass, updateNewStudentRegistration } from '../services/firebaseService';
```

- [ ] **Step 2.1: `updateNewStudentRegistration`이 firebaseService에서 export되어 있는지 확인**

```bash
grep -n "export const updateNewStudentRegistration" "src/services/firebaseService.js"
```

Expected: 한 줄 이상 매치. 없으면 다음 export를 `firebaseService.js`에 추가:

```javascript
export const updateNewStudentRegistration = async (id, updates) => {
    return safeWrite(async () => {
        await updateDoc(doc(db, 'newStudentRegistrations', id), {
            ...updates,
            updatedAt: serverTimestamp()
        });
        return { success: true };
    });
};
```

(이미 `CoachNewStudents.jsx`에서 동일 함수를 import해 쓰므로 export되어 있을 가능성 높음. 확인용 step.)

- [ ] **Step 3: 수동 검증**

`npm run dev` 새로고침. 다음 시나리오로 테스트:

**3-1. 입학반 연결 + 네이버 결제**: 신규 모드 + 핸드폰 입력 + 입학반 선택 + 결제방식 "네이버" → 등록. 학생 핸드폰으로 SMS 2 도착 (승인+시간표+네이버 결제 링크 포함). Firebase 콘솔의 새 registration 문서에 `reminderGroupId` 필드가 저장됨 (입학반이 3일 이상 남은 경우).

**3-2. 입학반 연결 + 현장 결제**: 결제방식을 "카드" 또는 "계좌"로 → SMS 2의 결제 안내가 "입학반날 방문하셔서 결제해주세요!"

**3-3. 입학반 미선택**: 입학반 셀렉터 "선택 안 함" → SMS 2만 도착(입학반 정보 줄/리마인더 없음). `reminderGroupId` 저장 안 됨.

**3-4. 핸드폰 미입력**: SMS 발송 자체 스킵. 등록은 정상 완료.

- [ ] **Step 4: 커밋**

```bash
git add src/components/StudentRegistrationModal.jsx src/services/firebaseService.js
git commit -m "feat: StudentRegistrationModal 신규 등록 시 승인/리마인더 SMS 발송"
```

(firebaseService.js에 변경이 없었으면 그 파일은 빼고 커밋)

---

## Task 6: CoachNewStudents — "+ 직접 등록" 버튼 + StudentRegistrationModal 연결

**목표:** 입학반 관리의 "수강생 추가" 모달에 "+ 직접 등록" 버튼을 추가. 클릭 시 입학반 prefill된 StudentRegistrationModal이 열림.

**Files:**
- Modify: `src/components/CoachNewStudents.jsx`

- [ ] **Step 1: StudentRegistrationModal import + state 추가**

`src/components/CoachNewStudents.jsx` 상단 import 블록(라인 33 근처) 다음에 추가:

```javascript
import StudentRegistrationModal from './StudentRegistrationModal';
```

state 블록(약 56–66 라인)에 추가:

```javascript
// "+ 직접 등록"으로 열린 StudentRegistrationModal 컨텍스트
const [directRegEntrance, setDirectRegEntrance] = useState(null);
```

- [ ] **Step 2: "수강생 추가" 모달 푸터에 "+ 직접 등록" 버튼 추가**

`src/components/CoachNewStudents.jsx`의 `cns-modal-actions` 블록(약 1370–1380 라인)을 다음으로 교체:

```jsx
<div className="cns-modal-actions">
    <button
        className="cns-modal-btn cancel"
        onClick={() => { setShowAddStudentModal(null); setSheetNewStudents([]); setSelectedNewStudents(new Set()); }}
    >
        취소
    </button>
    <button
        className="cns-modal-btn"
        style={{ background: '#10b981', color: '#fff' }}
        onClick={() => {
            const ec = showAddStudentModal;
            setShowAddStudentModal(null);
            setSheetNewStudents([]);
            setSelectedNewStudents(new Set());
            setDirectRegEntrance(ec);
        }}
    >
        + 직접 등록
    </button>
    <button
        className="cns-modal-btn save"
        onClick={handleAddStudentsToEntrance}
        disabled={selectedNewStudents.size === 0}
    >
        추가 ({selectedNewStudents.size}명)
    </button>
</div>
```

- [ ] **Step 3: StudentRegistrationModal 렌더링 추가**

`src/components/CoachNewStudents.jsx` 컴포넌트 return 블록 끝부분, 가장 바깥 `</div>` 직전에 다음 추가:

```jsx
{directRegEntrance && (
    <StudentRegistrationModal
        onClose={() => setDirectRegEntrance(null)}
        onSuccess={async () => {
            setDirectRegEntrance(null);
            await loadEntranceClasses();
        }}
        initialEntranceId={directRegEntrance.id}
    />
)}
```

위치: `{/* === 대기(만석) 수강 승인 모달 === */}` 블록과 같은 레벨(파일 약 1608 라인 근처). 즉 `cns-content` 다음, 컴포넌트 root `cns-container` `</div>` 직전.

- [ ] **Step 4: 수동 검증**

`npm run dev` 새로고침. 코치 → 신규 수강생 관리 → 입학반 관리 탭 → 다가오는 입학반 카드 "+" 클릭 → "수강생 추가" 모달 열림 → 푸터의 "+ 직접 등록" 버튼 클릭 → 다음 확인:
1. "수강생 추가" 모달 닫힘.
2. StudentRegistrationModal이 신규 모드로 열림.
3. 입학반 셀렉터의 기본값이 방금 클릭한 입학반(예: 5월 16일)으로 세팅됨.
4. 학생 정보 입력 후 "즉시 등록" → 모달 닫힘 + 입학반 관리 화면이 새로고침되며 해당 입학반 카드의 인원이 1 증가하고 학생 이름 태그가 추가됨.
5. SMS 도착 (Task 5 검증과 동일).

- [ ] **Step 5: 커밋**

```bash
git add src/components/CoachNewStudents.jsx
git commit -m "feat: 수강생 추가 모달에 + 직접 등록 버튼 + StudentRegistrationModal 연결"
```

---

## Task 7: 통합 회귀 검증

**목표:** 모든 변경이 함께 동작하는지 + 기존 흐름이 깨지지 않았는지 확인.

- [ ] **Step 1: 회귀 시나리오 체크리스트**

`npm run dev`로 다음 시나리오를 순서대로 점검:

1. **`?register=true` 학생 자가 등록 흐름** (변경 없어야 함)
   - URL `?register=true`로 접속 → 7단계 위자드 → 제출 → 학생/코치 SMS 1 도착 → 코치 승인 → SMS 2/3 도착.
2. **수강생 관리 → "+ 등록" 신규 (입학반 미선택)**
   - 등록 → 시트 행 + Firestore 문서 생성 + SMS 2만 도착(입학반 정보 없음).
3. **수강생 관리 → "+ 등록" 신규 (입학반 선택)**
   - 등록 → 입학반 currentCount 증가 + SMS 2 + SMS 3 예약.
4. **수강생 관리 → "+ 등록" 재등록**
   - 입학반 셀렉터 안 보임 + 기존 흐름 그대로 + SMS 발송 안 함.
5. **신규 수강생 관리 → 입학반 관리 → 다가오는 입학반 "+" → 기존 신규 학생 선택 → 추가**
   - 기존 동작 그대로 (시트 학생을 입학반에 배정).
6. **같은 모달 → "+ 직접 등록"**
   - StudentRegistrationModal 입학반 prefill 상태로 열림.
7. **지난 입학반 접힘**
   - 토글 펼침/접힘 정상 동작.
8. **수강생 추가 모달 필터**
   - 이미 지난 입학반 다녀온 학생이 새 입학반의 "수강생 추가" 모달에 안 뜸.

- [ ] **Step 2: 빌드 검증**

```bash
npm run build
```

Expected: 에러 없이 빌드 성공.

- [ ] **Step 3: 회귀 결과를 사용자에게 보고하고 머지 결정 받기**

회귀 시나리오 결과(통과/실패)를 사용자에게 보고. 모두 통과 시 사용자에게 "머지/PR 생성 진행 여부"를 물어봄. 실패 항목이 있으면 디버깅 후 해당 Task로 돌아감.

---

## Self-Review 체크 결과

- **Spec 커버리지**:
  - 변경 A1 (셀렉터) → Task 3
  - 변경 A2 (등록 시 연결) → Task 4
  - 변경 A3 (SMS) → Task 5
  - 변경 B1 ("+" 직접 등록) → Task 6
  - 변경 B2 (필터 강화) → Task 1
  - 변경 C (지난 입학반 접힘) → Task 2
  - 회귀 검증 → Task 7
- **Placeholder**: 없음.
- **타입/이름 일관성**: `linkedEntrance`, `linkedEntranceClassDate`, `createdRegId`, `selectedEntranceId`, `entranceClasses`, `directRegEntrance`, `showPastEntrance` 모두 동일 이름으로 일관 사용.
- **`updateNewStudentRegistration` 존재 여부**: Task 5 Step 2.1에서 확인하는 명시적 단계 포함.
