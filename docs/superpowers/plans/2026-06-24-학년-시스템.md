# 학년 시스템 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 훈련일지 누적 훈련량을 경험치로 환산해 초·중·고·대 13단계 "학년"으로 표시하는 과정 지향 성장 시스템을 추가한다.

**Architecture:** 기존 **티어 시스템과 1:1 미러**. 순수 로직은 `src/utils/grades.js`(↔`tiers.js`), Firebase 캐시·집계는 `firebaseService.js`의 `refreshStudentXP`/`getGradeMap`/`backfillGradesForStudents`(↔`refreshStudentTier`/`getTierMap`/`backfillTiersForMonth`), 뱃지는 `GradeBadge.jsx`(↔`TierBadge.jsx`). 학년은 `users/{이름}.grade`에 캐시 → 게시판 뱃지·인사말은 추가 읽기 0. PR 갱신은 학년과 분리된 축하 팝업.

**Tech Stack:** React 19, Vite, Firebase Firestore(client SDK), Recharts, Vitest.

## Global Constraints

- **디자인**: 그라데이션 금지. 단일 코발트 액센트 `--accent #329BE7`. 상태칩 패턴 `{색}1A`/`보더{색}4D`/`텍스트{색}`. 라디우스 사다리값만(`--r-chip 8` 등). 본문 텍스트 `var(--text)`(순수 #000 금지).
- **XP 단위**: kg (정수). 화면 표시만 `t`(=1000kg) 또는 천단위 콤마.
- **성별 보정**: 여성 훈련량 **×1.5** (고정 상수 `FEMALE_COEF = 1.5`).
- **학년 곡선(진입 누적 XP, kg)**: 초1=0, 초2=1000, 초3=2500, 초4=5000, 초5=8000, 초6=13000, 중1=20000, 중2=30000, 중3=45000, 고1=65000, 고2=95000, 고3=135000, 대학=200000.
- **훈련량 정의**: record `sets[]` 중 `intensity.unit==='kg' && reps.unit==='회'`인 세트의 `intensity.value × reps.value` 합. (맨몸/시간 세트는 Phase 1 제외 — 검증된 기존 `getAttendanceRanking` 로직과 동일)
- **Git**: 작업 전 `git pull`. 커밋 메시지 한글. 이미 `feat/학년-시스템` 브랜치에서 작업 중.
- **테스트 glob**: vitest는 `src/**/*.test.js` 포함. `npm run test`로 실행.

---

## File Structure

- `src/utils/grades.js` — **신규**. 순수 로직(GRADES 테이블, xpToGrade, gradeProgress, recordVolume, computeUserXp). Firebase/DOM 비의존.
- `src/utils/grades.test.js` — **신규**. 위 로직 vitest.
- `src/services/firebaseService.js` — **수정**. `refreshStudentXP`, `getGradeMap`/`clearGradeMapCache`, `backfillGradesForStudents`, PR pending 헬퍼 추가.
- `src/components/GradeBadge.jsx` — **신규**. 학년 칩(↔TierBadge).
- `src/components/GradeHero.jsx` — **신규**. 인사말 아래 학년 칩 + 경험치바(탭→랭킹).
- `src/components/Dashboard.jsx` — **수정**. 인사말에 GradeHero, effect에 XP refresh/backfill, gradeMap 보드 전달.
- `src/board/PostList.jsx`, `PostDetail.jsx`, `CommentItem.jsx` — **수정**. `gradeMap` prop으로 GradeBadge 표시.
- `src/components/Ranking.jsx` — **수정**. graph 탭에 누적 경험치 곡선 + 학년 사다리 추가.
- `src/components/PRSubmitModal.jsx` — **수정**. 제출 결과로 축하 팝업, 코치 입력 시 pending 플래그.

---

## Task 1: 순수 로직 `grades.js` + 테스트

**Files:**
- Create: `src/utils/grades.js`
- Test: `src/utils/grades.test.js`

**Interfaces:**
- Produces:
  - `GRADES: Array<{key, label, short, group, min}>` (낮은→높은, min=진입 누적 XP kg)
  - `FEMALE_COEF: number` (1.5)
  - `xpToGrade(xp: number) => GRADES[i]`
  - `gradeProgress(xp: number) => {grade, next|null, pct: number, remaining: number}`
  - `recordVolume(record) => number` (kg×회 raw 합)
  - `computeUserXp(records: object[], gender: string) => number` (Σ recordVolume × (gender==='여'?1.5:1))

- [ ] **Step 1: 실패하는 테스트 작성**

```js
// src/utils/grades.test.js
import { describe, it, expect } from 'vitest';
import { GRADES, xpToGrade, gradeProgress, recordVolume, computeUserXp, FEMALE_COEF } from './grades';

describe('xpToGrade', () => {
  it('0 XP는 초등1', () => expect(xpToGrade(0).short).toBe('초1'));
  it('경계 정확: 20000kg는 중등1', () => expect(xpToGrade(20000).short).toBe('중1'));
  it('경계 직전: 19999kg는 초등6', () => expect(xpToGrade(19999).short).toBe('초6'));
  it('최상단 초과: 250000kg는 대학', () => expect(xpToGrade(250000).short).toBe('대학'));
  it('GRADES는 min 오름차순(단조 누진)', () => {
    for (let i = 1; i < GRADES.length; i++) expect(GRADES[i].min).toBeGreaterThan(GRADES[i - 1].min);
  });
});

describe('gradeProgress', () => {
  it('중간값 진척: 26500kg(중1~중2 사이 절반)', () => {
    const p = gradeProgress(26500); // 중1=20000, 중2=30000 → (26500-20000)/10000 = 65%
    expect(p.grade.short).toBe('중1');
    expect(p.next.short).toBe('중2');
    expect(Math.round(p.pct)).toBe(65);
    expect(p.remaining).toBe(3500);
  });
  it('대학(졸업)은 next 없음, pct 100', () => {
    const p = gradeProgress(200000);
    expect(p.next).toBe(null);
    expect(p.pct).toBe(100);
  });
});

describe('recordVolume / computeUserXp', () => {
  const rec = (sets) => ({ sets });
  it('kg×회 세트만 합산', () => {
    expect(recordVolume(rec([
      { intensity: { value: '100', unit: 'kg' }, reps: { value: '5', unit: '회' } }, // 500
      { intensity: { value: '60', unit: 'kg' }, reps: { value: '90', unit: '초' } }, // 시간세트 제외
    ]))).toBe(500);
  });
  it('손상된 sets(배열 아님)는 0', () => expect(recordVolume({ sets: null })).toBe(0));
  it('여성은 ×1.5', () => {
    const recs = [rec([{ intensity: { value: '100', unit: 'kg' }, reps: { value: '10', unit: '회' } }])]; // 1000
    expect(computeUserXp(recs, '남')).toBe(1000);
    expect(computeUserXp(recs, '여')).toBe(1500);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm run test -- src/utils/grades.test.js`
Expected: FAIL ("Cannot find module './grades'" 또는 import 에러)

- [ ] **Step 3: 최소 구현 작성**

```js
// src/utils/grades.js
// 학년 = 누적 보정 훈련량(XP, kg) 버킷. 티어(tiers.js)와 동일한 형태의 순수 로직.
// min = 해당 학년 "진입" 누적 XP. 곡선은 6.5개월 실데이터로 보정한 누진 곡선(운영 손잡이).
export const GRADES = [
    { key: 'e1', short: '초1', label: '초등 1학년', group: '초등', min: 0 },
    { key: 'e2', short: '초2', label: '초등 2학년', group: '초등', min: 1000 },
    { key: 'e3', short: '초3', label: '초등 3학년', group: '초등', min: 2500 },
    { key: 'e4', short: '초4', label: '초등 4학년', group: '초등', min: 5000 },
    { key: 'e5', short: '초5', label: '초등 5학년', group: '초등', min: 8000 },
    { key: 'e6', short: '초6', label: '초등 6학년', group: '초등', min: 13000 },
    { key: 'm1', short: '중1', label: '중등 1학년', group: '중등', min: 20000 },
    { key: 'm2', short: '중2', label: '중등 2학년', group: '중등', min: 30000 },
    { key: 'm3', short: '중3', label: '중등 3학년', group: '중등', min: 45000 },
    { key: 'h1', short: '고1', label: '고등 1학년', group: '고등', min: 65000 },
    { key: 'h2', short: '고2', label: '고등 2학년', group: '고등', min: 95000 },
    { key: 'h3', short: '고3', label: '고등 3학년', group: '고등', min: 135000 },
    { key: 'u', short: '대학', label: '대학(졸업)', group: '대학', min: 200000 },
];

export const FEMALE_COEF = 1.5;

const numVal = (v) => { const n = parseFloat(v); return Number.isFinite(n) ? n : 0; };

export function gradeByKey(key) {
    return GRADES.find(g => g.key === key) || null;
}

// 누적 XP → 학년(위에서부터 첫 매칭). tiers.scoreToTier와 동일 패턴.
export function xpToGrade(xp) {
    const x = Number(xp) || 0;
    let g = GRADES[0];
    for (const cand of GRADES) if (x >= cand.min) g = cand;
    return g;
}

// 다음 학년까지 진척. 최상단은 next=null, pct=100.
export function gradeProgress(xp) {
    const x = Number(xp) || 0;
    const grade = xpToGrade(x);
    const idx = GRADES.indexOf(grade);
    const next = idx < GRADES.length - 1 ? GRADES[idx + 1] : null;
    if (!next) return { grade, next: null, pct: 100, remaining: 0 };
    const span = next.min - grade.min;
    const into = x - grade.min;
    return { grade, next, pct: (into / span) * 100, remaining: Math.max(0, next.min - x) };
}

// record 1건의 kg×회 훈련량(성별 보정 전). getAttendanceRanking과 동일 규칙.
export function recordVolume(record) {
    const sets = Array.isArray(record?.sets) ? record.sets : [];
    let v = 0;
    for (const s of sets) {
        if (s?.intensity?.unit === 'kg' && s?.reps?.unit === '회') {
            v += numVal(s.intensity?.value) * numVal(s.reps?.value);
        }
    }
    return v;
}

// 본인 records 전량 → 보정 누적 XP.
export function computeUserXp(records, gender) {
    const coef = gender === '여' ? FEMALE_COEF : 1;
    let total = 0;
    for (const r of (records || [])) total += recordVolume(r);
    return Math.round(total * coef);
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm run test -- src/utils/grades.test.js`
Expected: PASS (전부 통과)

- [ ] **Step 5: 커밋**

```bash
git add src/utils/grades.js src/utils/grades.test.js
git commit -m "feat(학년): 학년 순수 로직(GRADES 곡선/xpToGrade/진척/훈련량 환산) + 테스트"
```

---

## Task 2: Firebase XP 캐시·집계 (`firebaseService.js`)

**Files:**
- Modify: `src/services/firebaseService.js` (티어 블록 직후, 약 `:1875` 부근에 추가)

**Interfaces:**
- Consumes: `grades.js`의 `computeUserXp`, `xpToGrade`; 기존 `queryDocs`, `where`, `doc`, `getDoc`, `setDoc`, `getDocs`, `collection`, `writeBatch`, `serverTimestamp`, `safeRead`, `HOLIDAY_CACHE_TTL_MS`.
- Produces:
  - `refreshStudentXP({userName, gender}) => {xp, grade, isNew}` — 본인 records 전량 재계산 후 user 문서 저장.
  - `getGradeMap() => {[이름]: gradeKey}` (5분 캐시)
  - `clearGradeMapCache()`
  - `backfillGradesForStudents(students) => gradeMap` — 코치 1회, 전체 records 1회 읽어 batch write.

- [ ] **Step 1: import에 grades 로직 추가**

`firebaseService.js` 상단 import 구역(기존 `import { scoreToTier, ... } from '../utils/tiers'` 인근)에 추가:

```js
import { computeUserXp, xpToGrade } from '../utils/grades';
```

- [ ] **Step 2: XP 서비스 함수 추가**

`isMonthlyStampDone` 함수 정의 **앞**(티어 블록 끝, `:1876` 부근)에 삽입:

```js
// ============================================
// 학년(누적 훈련량 경험치) — 티어 패턴 미러
// ============================================

// 본인 records 전량 재계산 → users/{이름}에 xp/grade 저장. (본인 데이터로 경계됨, 폭증 아님)
export const refreshStudentXP = async ({ userName, gender }) => {
    return safeRead({ xp: 0, grade: null, isNew: false }, async () => {
        const name = (userName || '').trim();
        if (!name) return { xp: 0, grade: null, isNew: false };
        const userRef = doc(db, 'users', name);
        const snap = await getDoc(userRef);
        if (!snap.exists()) return { xp: 0, grade: null, isNew: false };
        const u = snap.data() || {};
        const records = await queryDocs('records', where('userName', '==', name));
        const xp = computeUserXp(records, gender);
        const grade = xpToGrade(xp).key;
        const isNew = !u.grade; // 첫 계산이면 인트로 안내 대상
        const now = new Date();
        const ym = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}`;
        await setDoc(userRef, {
            xp, grade, gradeMonth: ym, gradeIntroPending: false, gradeUpdatedAt: serverTimestamp(),
        }, { merge: true });
        clearGradeMapCache();
        return { xp, grade, isNew };
    });
};

// 이름→학년키 맵(게시판 뱃지용). 5분 캐시. (getTierMap 미러)
let gradeMapCache = null;
let gradeMapFetchedAt = 0;
function clearGradeMapCache() {
    gradeMapCache = null;
    gradeMapFetchedAt = 0;
}
export const getGradeMap = async () => {
    return safeRead({}, async () => {
        const now = Date.now();
        if (gradeMapCache && now - gradeMapFetchedAt < HOLIDAY_CACHE_TTL_MS) return gradeMapCache;
        const snap = await getDocs(collection(db, 'users'));
        const map = {};
        snap.forEach(d => { const g = d.data()?.grade; if (g) map[d.id] = g; });
        gradeMapCache = map;
        gradeMapFetchedAt = now;
        return map;
    });
};

// 코치 1회: 이번 달 미계산 학생 전원의 학년을 일괄 계산. 전체 records 1회 읽어 이름별 그룹핑.
// (backfillTiersForMonth 미러. 성별은 students 행의 '성별' 컬럼 사용)
export const backfillGradesForStudents = async (students) => {
    return safeRead({}, async () => {
        if (!Array.isArray(students) || students.length === 0) return getGradeMap();
        const now = new Date();
        const ym = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}`;

        const usersSnap = await getDocs(collection(db, 'users'));
        const userMeta = {};
        usersSnap.forEach(d => { const x = d.data() || {}; userMeta[d.id] = { grade: x.grade || null, gradeMonth: x.gradeMonth }; });

        // 이름→성별 (시트 행)
        const genderByName = new Map();
        for (const s of students) {
            const nm = (s['이름'] || '').trim();
            if (nm) genderByName.set(nm, (s['성별'] || '').trim());
        }
        const targets = [...genderByName.keys()].filter(nm => userMeta[nm] && userMeta[nm].gradeMonth !== ym);
        if (targets.length === 0) return getGradeMap();

        // 전체 records 1회 → 이름별 그룹핑(대소문자 무시)
        const all = await getDocs(collection(db, 'records'));
        const recByName = new Map();
        all.forEach(d => {
            const r = d.data();
            const key = (r.userName || '').trim().toLowerCase();
            if (!key) return;
            if (!recByName.has(key)) recByName.set(key, []);
            recByName.get(key).push(r);
        });

        const batch = writeBatch(db);
        for (const nm of targets) {
            const recs = recByName.get(nm.toLowerCase()) || [];
            const xp = computeUserXp(recs, genderByName.get(nm));
            const grade = xpToGrade(xp).key;
            batch.set(doc(db, 'users', nm), {
                xp, grade, gradeMonth: ym, gradeIntroPending: true, gradeUpdatedAt: serverTimestamp(),
            }, { merge: true });
        }
        await batch.commit();
        clearGradeMapCache();
        return getGradeMap();
    });
};
```

- [ ] **Step 3: 빌드/린트로 참조·문법 확인**

Run: `npm run lint`
Expected: 새 코드에 에러 없음(기존 경고는 무시). `computeUserXp`/`xpToGrade`/`writeBatch` 등 import 해결됨.

- [ ] **Step 4: 커밋**

```bash
git add src/services/firebaseService.js
git commit -m "feat(학년): 본인 기록 전량 재계산 XP 캐시 + 게시판용 학년맵/코치 백필"
```

---

## Task 3: 학년 뱃지 `GradeBadge.jsx`

**Files:**
- Create: `src/components/GradeBadge.jsx`

**Interfaces:**
- Consumes: `grades.js`의 `gradeByKey`.
- Produces: `<GradeBadge grade={gradeKey} style={...} />` — grade 없으면 null.

- [ ] **Step 1: 컴포넌트 작성 (TierBadge 미러, 코발트 단색)**

```jsx
// src/components/GradeBadge.jsx
import { gradeByKey } from '../utils/grades';

// 이름 앞 학년 칩. grade 없으면(코치/미계산) 아무것도 안 그림.
// 디자인: 단일 코발트 액센트. 상태칩 패턴(배경 1A / 보더 4D / 텍스트 accent).
const ACCENT = '#329BE7';
export default function GradeBadge({ grade, style }) {
    const g = gradeByKey(grade);
    if (!g) return null;
    return (
        <span
            title={`${g.label}`}
            style={{
                display: 'inline-flex', alignItems: 'center', gap: '2px',
                padding: '0 5px', height: '16px', borderRadius: 'var(--r-chip, 8px)',
                fontSize: '0.65rem', fontWeight: 700, lineHeight: 1,
                color: ACCENT, background: `${ACCENT}1A`, border: `1px solid ${ACCENT}4D`,
                verticalAlign: 'middle', flexShrink: 0, marginRight: '4px',
                ...style,
            }}
        >
            🎓 {g.short}
        </span>
    );
}
```

- [ ] **Step 2: 임시 렌더 확인**

Run: `npm run lint`
Expected: 에러 없음. (시각 확인은 Task 5에서 게시판에 붙인 뒤 `npm run dev`)

- [ ] **Step 3: 커밋**

```bash
git add src/components/GradeBadge.jsx
git commit -m "feat(학년): 학년 뱃지 컴포넌트(코발트 칩)"
```

---

## Task 4: 인사말 학년 히어로 + Dashboard 연결

**Files:**
- Create: `src/components/GradeHero.jsx`
- Modify: `src/components/Dashboard.jsx` (import; effect 약 `:55-70`; gradeMap state; 인사말 `:394`)

**Interfaces:**
- Consumes: `grades.js`의 `gradeProgress`, `gradeByKey`; firebaseService의 `refreshStudentXP`, `getGradeMap`, `backfillGradesForStudents`.
- Produces: `<GradeHero xp={number} onClick={fn} />`.

- [ ] **Step 1: GradeHero 컴포넌트 작성**

```jsx
// src/components/GradeHero.jsx
import { gradeProgress } from '../utils/grades';

const ACCENT = '#329BE7';
// 인사말 아래 학년 칩 + 경험치바. 탭하면 성장 화면으로.
export default function GradeHero({ xp = 0, onClick }) {
    const { grade, next, pct, remaining } = gradeProgress(xp);
    return (
        <button
            onClick={onClick}
            style={{
                display: 'block', width: '100%', textAlign: 'left', cursor: 'pointer',
                background: 'transparent', border: 'none', padding: '6px 0 0', font: 'inherit',
            }}
        >
            <div style={{ display: 'flex', alignItems: 'center', gap: '6px', marginBottom: '6px' }}>
                <span style={{
                    fontSize: '0.78rem', fontWeight: 700, color: ACCENT,
                    background: `${ACCENT}1A`, border: `1px solid ${ACCENT}4D`,
                    borderRadius: 'var(--r-chip, 8px)', padding: '2px 8px',
                }}>🎓 {grade.label}</span>
            </div>
            <div style={{ height: '8px', borderRadius: '6px', background: 'var(--hairline, #EFEFF0)', overflow: 'hidden' }}>
                <div style={{ height: '100%', width: `${pct}%`, background: ACCENT, borderRadius: '6px' }} />
            </div>
            <div style={{ fontSize: '0.72rem', color: 'var(--text-muted, #A7A7AA)', marginTop: '5px' }}>
                {next
                    ? `다음 학년까지 ${remaining.toLocaleString()}kg · ${Math.round(pct)}%`
                    : '🎓 졸업 — 최고 학년 달성!'}
            </div>
        </button>
    );
}
```

- [ ] **Step 2: Dashboard import 추가**

`Dashboard.jsx` import 구역에서 firebaseService import에 `getGradeMap, backfillGradesForStudents, refreshStudentXP` 추가하고, 컴포넌트 import 추가:

```jsx
import GradeHero from './GradeHero';
```

(기존 `import { ... getTierMap, refreshStudentTier, backfillTiersForMonth } from '../services/firebaseService';` 줄의 목록에 세 함수 이름을 추가한다.)

- [ ] **Step 3: gradeMap state + xp state 추가**

`Dashboard.jsx`에서 `tierMap` state 선언 인근에 추가:

```jsx
const [gradeMap, setGradeMap] = useState({});
const [myXp, setMyXp] = useState(0);
```

- [ ] **Step 4: effect에서 XP refresh/backfill 호출**

기존 티어 effect(약 `:55-70`)와 같은 블록 안에 미러로 추가. 코치 분기(`backfillTiersForMonth(students)...` 인근)에:

```jsx
backfillGradesForStudents(students).then(map => { if (!cancel && map) setGradeMap(map); });
```

학생 분기(`refreshStudentTier({ userName: user.username })...` 인근)에:

```jsx
const myGender = (students.find(s => (s['이름'] || '').trim() === user.username)?.['성별'] || '').trim();
refreshStudentXP({ userName: user.username, gender: myGender }).then(res => {
    if (!cancel && res) setMyXp(res.xp);
});
getGradeMap().then(map => { if (!cancel && map) setGradeMap(map); });
```

(`students`, `user`, `cancel`은 기존 effect 스코프에 이미 존재. 없으면 기존 티어 호출이 쓰는 동일 변수를 사용.)

- [ ] **Step 5: 인사말에 GradeHero 삽입**

`Dashboard.jsx:394` 인사말 블록을 학생일 때 GradeHero가 따라오도록 수정. 기존:

```jsx
환영합니다, {user.role !== 'coach' && <TierBadge tier={tierMap[user.username]} style={{ height: '20px', fontSize: '0.75rem' }} />}{user.username}님
```

이 인사말을 감싸는 컨테이너 바로 아래(학생만)에 추가:

```jsx
{user.role !== 'coach' && <GradeHero xp={myXp} onClick={() => setCurrentPage && setCurrentPage('ranking')} />}
```

> Dashboard가 페이지 전환 함수를 prop으로 받지 않으면, 랭킹 진입에 쓰는 기존 핸들러(대시보드의 랭킹 카드 onClick과 동일 함수)를 재사용한다. 해당 함수명을 grep으로 확인: `grep -n "ranking" src/components/Dashboard.jsx`.

- [ ] **Step 6: 실행 시각 확인**

Run: `npm run dev` 후 학생 계정으로 로그인 → 게시판 상단에 "🎓 OO학년" 칩 + 경험치바 + "다음 학년까지 …kg" 표시, 탭 시 랭킹 이동.
Expected: 콘솔 에러 없음. 심성희 계정이면 "고등 3학년" 표시.

- [ ] **Step 7: 커밋**

```bash
git add src/components/GradeHero.jsx src/components/Dashboard.jsx
git commit -m "feat(학년): 인사말에 학년 칩+경험치바(탭→성장), XP refresh/백필 연결"
```

---

## Task 5: 게시판 이름 옆 학년 뱃지

**Files:**
- Modify: `src/components/Dashboard.jsx` (gradeMap을 PostList에 전달)
- Modify: `src/board/PostList.jsx`, `src/board/PostDetail.jsx`, `src/board/CommentItem.jsx`

**Interfaces:**
- Consumes: `gradeMap` prop, `GradeBadge`.
- Produces: 기존 `tierMap`/`TierBadge`가 렌더되는 모든 위치에 `GradeBadge`를 **앞에** 추가.

- [ ] **Step 1: 전달 경로 파악**

Run: `grep -n "tierMap\|TierBadge" src/board/PostList.jsx src/board/PostDetail.jsx src/board/CommentItem.jsx src/components/Dashboard.jsx`
Expected: `tierMap`이 prop으로 흐르는 위치 목록. 각 위치마다 동일 패턴으로 `gradeMap`/`GradeBadge` 병행 추가.

- [ ] **Step 2: PostList에 GradeBadge 추가**

`PostList.jsx`에서 `import TierBadge` 옆에 `import GradeBadge from '../components/GradeBadge';` 추가. props에 `gradeMap` 추가. 작성자 이름 렌더에서 `<TierBadge tier={tierMap[name]} />` **앞**에:

```jsx
<GradeBadge grade={gradeMap?.[name]} />
```

(`name`은 해당 위치에서 쓰는 작성자 이름 변수명에 맞춘다.)

- [ ] **Step 3: PostDetail·CommentItem 동일 적용**

`PostDetail.jsx`, `CommentItem.jsx`에도 동일하게: import 추가, `gradeMap` prop 받기, `TierBadge` 앞에 `<GradeBadge grade={gradeMap?.[name]} />`.

- [ ] **Step 4: Dashboard에서 gradeMap 주입**

`Dashboard.jsx`에서 PostList를 렌더하는 곳에 `tierMap` 옆으로 `gradeMap={gradeMap}` 추가(PostDetail로의 전달 경로도 동일).

- [ ] **Step 5: 시각 확인**

Run: `npm run dev` → 게시판 글/댓글 작성자 이름 앞에 **학년 뱃지(파랑) + 티어 뱃지** 둘 다 표시.
Expected: 코치 이름엔 둘 다 없음(미계산). 빽빽하면 추후 조정(스펙에 명시).

- [ ] **Step 6: 커밋**

```bash
git add src/board/PostList.jsx src/board/PostDetail.jsx src/board/CommentItem.jsx src/components/Dashboard.jsx
git commit -m "feat(학년): 게시판/댓글 이름 옆 학년 뱃지(티어와 병행)"
```

---

## Task 6: 성장 화면 — 누적 경험치 곡선 + 학년 사다리

**Files:**
- Modify: `src/components/Ranking.jsx` (graph 탭에 두 블록 추가)

**Interfaces:**
- Consumes: `getMonthlyAttendanceHistory(userName)`(기존), `grades.js`의 `GRADES`, `FEMALE_COEF`, `xpToGrade`, `gradeProgress`; Recharts `ComposedChart/Line/ReferenceLine/XAxis/YAxis/Tooltip`.
- Produces: graph 탭 상단 2개 블록.

- [ ] **Step 1: 누적 경험치 곡선 블록 추가**

graph 탭(`activeTab === 'graph'`) 렌더 최상단에 삽입. 월별 운동량을 누적합 + 성별 보정해 라인, 학년 경계는 `ReferenceLine`.

```jsx
// monthly: getMonthlyAttendanceHistory(user.username) 결과 [{month, volume}], gender: '여'|'남'
// 누적 경험치 데이터 만들기 (성별 보정)
const coef = myGender === '여' ? FEMALE_COEF : 1;
let acc = 0;
const cumData = monthly.map(m => { acc += m.volume * coef; return { month: m.month.slice(2), xp: Math.round(acc) }; });
// 표시할 학년 경계(현재 도달 부근만)
const maxXp = cumData.length ? cumData[cumData.length - 1].xp : 0;
const bands = GRADES.filter(g => g.min > 0 && g.min <= maxXp * 1.4);
```

```jsx
<div className="ranking-card">
  <h3>누적 경험치 곡선</h3>
  <ResponsiveContainer width="100%" height={220}>
    <ComposedChart data={cumData} margin={{ top: 10, right: 12, left: 0, bottom: 0 }}>
      <XAxis dataKey="month" tick={{ fontSize: 11 }} />
      <YAxis tick={{ fontSize: 11 }} width={48} tickFormatter={(v) => `${Math.round(v / 1000)}t`} />
      <Tooltip formatter={(v) => `${Number(v).toLocaleString()}kg`} />
      {bands.map(g => (
        <ReferenceLine key={g.key} y={g.min} stroke="#EFEFF0" strokeDasharray="3 3"
          label={{ value: g.short, position: 'right', fontSize: 10, fill: '#A7A7AA' }} />
      ))}
      <Line type="monotone" dataKey="xp" stroke="#329BE7" strokeWidth={2.5} dot={false} />
    </ComposedChart>
  </ResponsiveContainer>
</div>
```

(이미 import된 Recharts 심볼을 재사용. 누락분 `ReferenceLine`은 기존 `import { ... } from 'recharts'`에 추가.)

- [ ] **Step 2: 학년 사다리 블록 추가**

곡선 블록 아래에 13단계 막대 + "YOU" 마커. `gradeProgress(maxXp)`로 현재 학년 판정.

```jsx
{(() => {
  const cur = xpToGrade(maxXp);
  return (
    <div className="ranking-card">
      <h3>학년 사다리</h3>
      <div style={{ display: 'flex', alignItems: 'flex-end', gap: '3px', height: '90px' }}>
        {GRADES.map((g, i) => {
          const reached = maxXp >= g.min;
          const isCur = g.key === cur.key;
          return (
            <div key={g.key} style={{ flex: 1, textAlign: 'center' }}>
              {isCur && <div style={{ fontSize: '9px', color: '#329BE7', fontWeight: 700 }}>YOU</div>}
              <div title={g.label} style={{
                height: `${20 + i * 5}px`, borderRadius: '3px',
                background: reached ? '#329BE7' : '#F7F7F8',
                border: reached ? 'none' : '1px dashed #A7A7AA',
              }} />
            </div>
          );
        })}
      </div>
      <div style={{ fontSize: '10px', color: '#A7A7AA', textAlign: 'center', marginTop: '4px' }}>
        초등 → 중등 → 고등 → 대학(졸업)
      </div>
    </div>
  );
})()}
```

- [ ] **Step 3: myGender / monthly 확보 확인**

graph 탭에서 `user.username`의 월별 데이터(`monthly`)와 성별(`myGender`)이 스코프에 있는지 확인. 없으면 graph 탭 진입 시 `getMonthlyAttendanceHistory(user.username)`를 호출하는 기존 effect 결과를 재사용하고, 성별은 Ranking이 이미 쓰는 `genderMap`(Ranking.jsx 내 성별맵)에서 `genderMap[user.username]`로 얻는다.

Run: `grep -n "genderMap\|getMonthlyAttendanceHistory\|ResponsiveContainer\|ReferenceLine" src/components/Ranking.jsx`

- [ ] **Step 4: 시각 확인**

Run: `npm run dev` → 랭킹 → 성장(graph) 탭. 상단에 누적 경험치 곡선(학년 경계선) + 학년 사다리(YOU 마커), 그 아래 기존 월별/PR 그래프 유지.
Expected: 콘솔 에러 없음. 곡선이 우상향, 경계선 라벨 표시.

- [ ] **Step 5: 커밋**

```bash
git add src/components/Ranking.jsx
git commit -m "feat(학년): 성장 화면에 누적 경험치 곡선 + 학년 사다리 추가"
```

---

## Task 7: PR 축하 팝업 (학년과 분리)

**Files:**
- Modify: `src/components/PRSubmitModal.jsx` (handleSubmit `:53-106`)
- Modify: `src/services/firebaseService.js` (pending 헬퍼 + submitPersonalBest milestone 판정)
- Modify: `src/components/Dashboard.jsx` 또는 학생 진입 지점 (코치 입력분 pending 팝업 1회)

**Interfaces:**
- Consumes: `submitPersonalBest`(기존 `{updated}` 반환), PR 통과 기준 맵.
- Produces:
  - `PR_REP_THRESHOLDS: {[exercise]: number}` (통과 기준)
  - `submitPersonalBest` 반환 확장 `{updated, milestone: boolean}` (반복 기준형이 기준 도달 시 true)
  - `setPRCelebrationPending(userName, payload)` / 학생 접속 시 소비.

- [ ] **Step 1: 통과 기준 상수 추가**

`src/data/mockData.js`(또는 `grades.js` 인접 새 상수 파일)에:

```js
// PR 반복 기준형 "다음 중량으로" 통과 기준 (종목명 정확 일치)
// ⚠️ 종목명 변경 시 깨짐 — 운영 손잡이. 필요 시 코치 편집 UI는 후순위.
export const PR_REP_THRESHOLDS = {
    '12분 맨메이커': 200,
    '5분 한팔 스윙': 100,
    '5분 하이풀': 100,
    '5분 스내치': 100,
};
```

- [ ] **Step 2: submitPersonalBest에 milestone 판정 추가**

`firebaseService.js`의 `submitPersonalBest`에서 `weightThenReps`이고 신기록(reps 갱신)일 때 통과 기준 도달 여부를 반환에 포함:

```js
// 함수 상단 import: import { PR_REP_THRESHOLDS } from '../data/mockData';
// 저장 직후 반환부 수정:
const threshold = PR_REP_THRESHOLDS[exercise];
const milestone = prType === 'weightThenReps' && threshold != null
    && Number(reps?.value) >= threshold;
return { docId, updated, milestone };
```

- [ ] **Step 3: PRSubmitModal에서 축하 분기**

`handleSubmit`의 `submitPersonalBest(...)` 결과 사용부:

```jsx
const res = await submitPersonalBest(payload);
// 본인이 입력한 경우 즉시 축하
if (res.milestone) showCelebration(`🏆 ${exercise} 기준 통과! 다음 중량으로 올라가도 좋아요!`);
else if (res.updated) showCelebration('🎉 신기록 축하합니다!');
// 코치가 학생 대신 입력한 경우 → 학생 다음 접속 때 보이도록 플래그
if (isCoachEntry && (res.updated || res.milestone)) {
    await setPRCelebrationPending(selectedStudent, {
        exercise, kind: res.milestone ? 'milestone' : 'record',
    });
}
```

`showCelebration`은 모달 내 간단한 토스트/오버레이 state(`const [celebration, setCelebration] = useState(null)`)로 구현하고, 값이 있으면 모달 위에 코발트 배너로 1.8초 표시 후 닫기.

- [ ] **Step 4: pending 헬퍼 + 학생 소비**

`firebaseService.js`에 추가:

```js
export const setPRCelebrationPending = async (userName, payload) => {
    const name = (userName || '').trim();
    if (!name) return;
    await setDoc(doc(db, 'users', name), {
        prCelebrationPending: { ...payload, at: serverTimestamp() },
    }, { merge: true });
};
export const consumePRCelebration = async (userName) => {
    return safeRead(null, async () => {
        const name = (userName || '').trim();
        const ref = doc(db, 'users', name);
        const snap = await getDoc(ref);
        const p = snap.exists() ? snap.data()?.prCelebrationPending : null;
        if (p) await updateDoc(ref, { prCelebrationPending: null });
        return p || null;
    });
};
```

Dashboard 학생 진입 effect(티어/XP refresh 인근)에서:

```jsx
consumePRCelebration(user.username).then(p => {
    if (!cancel && p) {
        const msg = p.kind === 'milestone'
            ? `🏆 ${p.exercise} 기준 통과! 다음 중량으로!`
            : `🎉 ${p.exercise} 신기록 축하합니다!`;
        // 기존 알림/모달 패턴으로 표시 (티어 인트로 모달과 동일한 표시 수단 재사용)
        setPrCelebration(msg);
    }
});
```

- [ ] **Step 5: 시각 확인**

Run: `npm run dev`
- 학생 본인이 PR 입력(반복 기준형, 기준 미달 신기록) → "🎉 신기록" 즉시.
- 기준 도달(예: 5분 스내치 100회) → "🏆 다음 중량으로" 즉시.
- 코치가 학생 대신 입력 → 그 학생으로 재로그인 시 축하 1회 후 사라짐.
Expected: 콘솔 에러 없음.

- [ ] **Step 6: 커밋**

```bash
git add src/components/PRSubmitModal.jsx src/services/firebaseService.js src/data/mockData.js src/components/Dashboard.jsx
git commit -m "feat(PR): 신기록/기준통과 축하 팝업(즉시) + 코치 대리입력은 학생 다음 접속 시"
```

---

## Task 8 (Phase 2, 선택): 체중 입력 + 맨몸운동 XP

> Phase 1은 검증된 kg×회 훈련량만 XP로 쓴다. 맨몸운동(체중×reps) 포함은 record의 맨몸 세트 스키마 확인 후 별도로 진행. 지금은 **스코프 밖**으로 두되, 착수 시:
> 1. `users/{이름}.bodyweight` 입력 카드를 `StudentInfo`(내 정보) 하단에 추가(`PasswordChangeCard` 패턴).
> 2. 훈련일지 record의 맨몸 세트 표기(예: `intensity.unit !== 'kg'` & reps.unit==='회') 확인.
> 3. `recordVolume`에 맨몸 분기 추가: 맨몸 세트는 `bodyweight × reps`. `recordVolume(record, {bodyweight})` 시그니처 확장, `computeUserXp(records, gender, bodyweight)`.
> 4. `grades.test.js`에 맨몸 환산 케이스 추가.
> 5. 곡선 재보정 검토(맨몸 포함 시 총량 상승).

---

## Self-Review

- **Spec coverage**: 학년 체계(Task1), XP 정의/성별보정(Task1), 곡선 보정값(Global Constraints+Task1), 성능 캐시·백필(Task2), 인사말 배치(Task4), 게시판 뱃지(Task5), 성장 그래프 4종(Task6: ①②신규, ③④는 기존 유지로 변경 없음), PR 축하 즉시+코치대리(Task7), 체중/맨몸(Task8 Phase2). 모든 스펙 항목에 대응 태스크 있음.
- **Placeholder scan**: 코드 스텝마다 실제 코드 포함. UI 연결은 grep으로 정확 위치 확인 지시. "적절히 처리" 류 없음.
- **Type consistency**: `xpToGrade`/`gradeProgress`/`recordVolume`/`computeUserXp` 시그니처가 Task1 정의와 Task2/4/6 사용처에서 일치. `getGradeMap`/`gradeMap`/`grade`(key) 명칭 일관. `submitPersonalBest` 반환 `{docId, updated, milestone}`로 확장—호출부(Task7) 일치.

## Execution Handoff

(아래 핸드오프 안내 참조)
